/**
 * Placeholder only — swap this out for the Login component/logic you've
 * already built. This exists so the route resolves and the app compiles
 * end to end.
 */
export default function Login() {
  return (
    <div style={{ maxWidth: 420, margin: '80px auto', padding: '0 20px' }}>
      <h1>Log in</h1>
      <p>Replace this with your existing login component.</p>
    </div>
  )
}
